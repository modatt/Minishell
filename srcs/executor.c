#include "minishell.h"

void    executor(t_command cmd_list)
{
    // check error invalid 
    cmd_validation(cmd_list);
    // parsing then execute 
}


bool    cmd_validation(t_command cmd_list)
{
    
}